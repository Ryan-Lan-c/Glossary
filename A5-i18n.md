# A5 - i18n / 國際化

← [返回索引(README.md)](./README.md)

---

## 目錄

- [A5 - i18n / 國際化](#a5---i18n--國際化)
  - [目錄](#目錄)
    - [i18n / l10n(術語)🟢](#i18n--l10n術語)
    - [Locale(語系)🟢](#locale語系)
    - [ResourceBundle / MessageSource 🟡](#resourcebundle--messagesource-)
    - [Locale 解析策略 🟡](#locale-解析策略-)
    - [日期 / 數字 / 貨幣格式化 🟢](#日期--數字--貨幣格式化-)
    - [前後端 i18n 分工 🟡](#前後端-i18n-分工-)

---

<a id="i18n-l10n"></a>
### i18n / l10n(術語)🟢

**定義**:
- **i18n**(Internationalization,國際化)— 讓**程式碼**具備支援多語系的**能力**(架構層面)
- **l10n**(Localization,在地化)— 為**特定語系**填入翻譯與格式調整(內容層面)
- 縮寫由來:`internationalization` 首尾之間有 18 個字母 → i18n;`localization` 中間 10 個字母 → l10n

**為什麼用**:把這兩件事拆開,工程師專注於「**抽出字串、用 Locale 物件、避免硬編碼日期格式**」(i18n);翻譯師獨立完成各語系內容(l10n),不需碰 code。

**範例**:
```java
// ❌ 沒做 i18n:字串硬編碼
String msg = "找不到該訂單";

// ✅ 做了 i18n:訊息抽到 properties
// messages.properties:        order.notfound=找不到該訂單
// messages_en.properties:     order.notfound=Order not found
String msg = messageSource.getMessage("order.notfound", null, locale);
```

**延伸**:`g11n`(globalization)= i18n + l10n + 文化 / 法規調整(更廣的概念,例如某些國家不能顯示某顏色或宗教符號)。

---

<a id="locale"></a>
### Locale(語系)🟢

**定義**:Java(以及多數語言)用來表示「**語言 + 區域**」的物件——例如「繁體中文(台灣)」、「英文(美國)」。標準遵循 IETF BCP 47,底層常見 ISO 639(語言碼)+ ISO 3166(國家碼)。

**為什麼用**:相同語言在不同地區仍有顯著差異——`zh-TW` 用繁體、`zh-CN` 用簡體;`en-US` 日期 `mm/dd/yyyy`、`en-GB` 用 `dd/mm/yyyy`。Locale 是 i18n 機制的「**主鍵**」:查訊息、選格式、選複數規則全靠它。

**常見 Locale**:

| Locale | 語言 | 區域 |
|---|---|---|
| `zh-TW` | 繁體中文 | 台灣 |
| `zh-CN` | 簡體中文 | 中國 |
| `en-US` | 英文 | 美國 |
| `en-GB` | 英文 | 英國 |
| `ja-JP` | 日文 | 日本 |

**範例**:
```java
Locale taiwan  = Locale.forLanguageTag("zh-TW");
Locale us      = Locale.US;                            // 預定義常數
Locale current = LocaleContextHolder.getLocale();      // Spring 取當前請求 Locale
```

**前端對照**:瀏覽器 `navigator.language`(`"zh-TW"`)、HTTP 帶 `Accept-Language: zh-TW,en-US;q=0.9`(`q` 是優先權)。

---

<a id="resource-bundle"></a>
### ResourceBundle / MessageSource 🟡

**定義**:把翻譯字串集中放在 `.properties` 檔的機制。Java 標準叫 `ResourceBundle`,Spring 包裝成更好用的 `MessageSource`。

**為什麼用**:i18n 的核心原則是「**程式碼裡不放任何使用者可見的自然語言字串**」,所有訊息靠 key 從外部檔案查出。`MessageSource` 還能:
- **多語系自動 fallback**(找不到 `zh-TW` → 找 `zh` → 找預設)
- **訊息參數替換**(`{0}`、`{1}`)
- **熱重載**(`ReloadableResourceBundleMessageSource`,改了 properties 不必重啟)

**檔案命名慣例**:
```
messages.properties           ← 預設(對不到 Locale 時 fallback)
messages_zh_TW.properties     ← 繁體中文
messages_en.properties        ← 英文
messages_ja_JP.properties     ← 日文
```

**Spring 範例**:
```java
@Configuration
public class MessageConfig {
    @Bean
    public MessageSource messageSource() {
        var ms = new ReloadableResourceBundleMessageSource();
        ms.setBasename("classpath:messages");
        ms.setDefaultEncoding("UTF-8");   // ⚠ Java 9 前 properties 預設 ISO-8859-1,要明設
        ms.setCacheSeconds(60);
        return ms;
    }
}

// 使用
String msg = messageSource.getMessage(
    "order.notfound", new Object[]{ orderId }, locale);
// messages_zh_TW.properties:  order.notfound=找不到訂單 {0}
```

**Quarkus 對照**:`@MessageBundle` + Qute 模板,API 不同但概念一致。

---

<a id="locale-resolver"></a>
### Locale 解析策略 🟡

**定義**:Server 怎麼決定「**這個 request 該用哪個 Locale**」—— Spring 用 `LocaleResolver` 介面抽象這件事。

**為什麼用**:不同情境需要不同來源——API 服務跟著瀏覽器走最自然(用 header)、面向人類的網站要讓使用者主動切換並記住(用 cookie)。

**Spring 內建策略**:

| Resolver | 來源 | 適用 |
|---|---|---|
| `AcceptHeaderLocaleResolver` | HTTP `Accept-Language` header | API 服務、跟著瀏覽器走 |
| `CookieLocaleResolver` | Cookie | 使用者主動切換、跨分頁一致 |
| `SessionLocaleResolver` | HttpSession | 有 Session 的傳統 Web 應用 |
| `FixedLocaleResolver` | 固定值 | 單一語系系統 |
| 自訂 | URL 前綴 `/en/foo` 或 query param | SEO 友善、語系顯式在 URL |

**搭配 `LocaleChangeInterceptor`(讓使用者可主動切換)**:
```java
@Bean
LocaleChangeInterceptor localeInterceptor() {
    var i = new LocaleChangeInterceptor();
    i.setParamName("lang");   // 任何 URL 帶 ?lang=en 就觸發切換
    return i;
}
```
使用者點「切英文」→ 跳到 `/?lang=en`,Resolver 寫入 cookie / session,之後請求都用 en。

**選型重點**:
- API 服務 → **AcceptHeader**(stateless、跟瀏覽器走)
- 面向人類的網站 → **Cookie + LocaleChangeInterceptor**

---

<a id="format"></a>
### 日期 / 數字 / 貨幣格式化 🟢

**定義**:同樣的 `2026-05-14` / `1234567.89` / `1000` 元,在不同 Locale 下要呈現成不同形式——靠 `java.time.format.DateTimeFormatter` / `java.text.NumberFormat`(底層資料來自 **CLDR**,Unicode Common Locale Data Repository)。

**為什麼用**:硬編碼 `"yyyy-MM-dd"` 不符合各國慣例;直接 `String.valueOf(num)` 不會自動加千分位、不會用對的小數點符號(德文用 `.` 當千分位、`,` 當小數點)。

**Java 範例**:
```java
LocalDateTime now = LocalDateTime.now();

// 日期格式
String tw = now.format(DateTimeFormatter
    .ofLocalizedDateTime(FormatStyle.MEDIUM)
    .withLocale(Locale.TAIWAN));
// "2026年5月14日 上午10:23"

String us = now.format(DateTimeFormatter
    .ofLocalizedDateTime(FormatStyle.MEDIUM)
    .withLocale(Locale.US));
// "May 14, 2026, 10:23:00 AM"

// 數字千分位
NumberFormat.getInstance(Locale.TAIWAN).format(1234567.89);  // "1,234,567.89"

// 貨幣
NumberFormat.getCurrencyInstance(Locale.TAIWAN).format(1000);  // "NT$1,000.00"
NumberFormat.getCurrencyInstance(Locale.JAPAN).format(1000);   // "¥1,000"
```

**前端對照**:
```javascript
new Intl.DateTimeFormat('zh-TW', { dateStyle: 'medium' }).format(new Date());
new Intl.NumberFormat('zh-TW', { style: 'currency', currency: 'TWD' }).format(1000);
```

**複數規則差異**:英文 `1 apple` / `2 apples`,中文無變化(`1 個蘋果` / `2 個蘋果`),阿拉伯文有 6 種複數形式(`zero` / `one` / `two` / `few` / `many` / `other`)。靠 `MessageFormat` 的 `ChoiceFormat`(Java)或 ICU MessageFormat(跨平台)處理。

---

<a id="frontend-backend"></a>
### 前後端 i18n 分工 🟡

**定義**:多語系系統中,**哪些字串歸後端管、哪些歸前端管**——SPA 時代必須先談清楚的事。

**典型分工**:

| 範疇 | 後端 | 前端 |
|---|:---:|:---:|
| API 錯誤訊息(`order.notfound`)| ✅ | |
| Email / SMS 模板 | ✅ | |
| 報表 / PDF 文字 | ✅ | |
| Server 端寄出的推播文字 | ✅ | |
| UI 按鈕 / Label / Placeholder | | ✅ |
| 表單驗證錯誤(前端先擋的) | | ✅ |
| 頁面標題 / Meta tag(SEO) | | ✅ |
| 導覽列 / 選單 | | ✅ |

**常見爭議**:
- **驗證錯誤訊息**:同一條規則(「Email 格式錯誤」)前後端都要驗 → **訊息共用?** 通常**前端短訊息自己寫,後端訊息獨立**(情境不一定一致)
- **時間格式化**:Server 回 ISO 8601(`2026-05-14T10:23Z`),由前端用 Locale 格式化 — **強烈推薦**(Server 不知道使用者的時區與偏好)

**前端常見 i18n 套件**:
- Vue:[`vue-i18n`](https://vue-i18n.intlify.dev/)
- React:[`react-i18next`](https://react.i18next.com/) / `formatjs`
- Angular:內建 `@angular/localize`

**載入策略**:

| 策略 | 適用 | 取捨 |
|---|---|---|
| **打包進 bundle** | 語系少、字串少 | bundle 變大,但無延遲 |
| **動態載入** | 語系多 | 切換時才下載對應語系包 |

> 📌 **個人實戰偏好**:目前大多將翻譯寫在前端 JS 檔內,**自行維護 key-value 對照**(使用 vue-i18n套件);後端不負責語系訊息。

---

← [返回索引(README.md)](./README.md)
